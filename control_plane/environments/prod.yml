# Deployment parameters for the PROD environment.
# Secrets (SP client id/secret, source DB password) come from CI secrets, never here.
workspace_base: HackathonShuo      # target workspace = <base>-<environment>
environment: PROD
tenant_id: 490b5e1f-5a00-41ad-bac3-4704c9c4042c
capacity_id: <PROD_CAPACITY_ID>    # set the PROD Fabric capacity id before deploying
